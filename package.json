{
  "name": "rip-tequila",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "hugo server --bind 127.0.0.1 --port 1313 --disableFastRender",
    "build": "hugo --gc --minify"
  },
  "devDependencies": {
    "sass-embedded": "^1.99.0"
  }
}
